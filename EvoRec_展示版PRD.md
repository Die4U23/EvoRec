# EvoRec 智能商品推荐平台

产品需求文档 PRD

版本 V1.0　｜　状态 需求设计　｜　更新日期 2026 年 9 月 14 日

EvoRec 面向持续更新的商品库，将用户兴趣建模、生成式检索与内容匹配相结合，提供个性化商品发现、新品推荐和效果分析能力。

本文用于产品方案展示与需求评审。文中的界面为设计示意，指标为规划目标，实际表现由上线前验收确认。

## 01 产品概述

### 产品定位

EvoRec 是一个面向动态商品库的个性化推荐平台。浏览者通过推荐流发现感兴趣的商品；运营人员通过商品工作台维护推荐供给；算法人员通过策略对比与效果看板管理推荐质量。

产品围绕“用户兴趣变化”和“商品持续更新”组织推荐：既利用已有行为理解用户偏好，也利用商品内容为缺少历史交互的新品建立推荐机会，并根据请求特点分配检索计算量。

### 业务问题

| 问题 | 典型表现 | 产品响应 |
| --- | --- | --- |
| 推荐与当前兴趣脱节 | 历史偏好占据列表，近期关注内容难以体现 | 将近期行为纳入下一次推荐输入 |
| 新品缺乏推荐机会 | 商品已经入库，但缺少行为数据 | 通过内容表示与动态索引参与候选检索 |
| 推荐效果难以解释和比较 | 只看到结果列表，难以判断策略差异 | 提供相同输入下的策略对比与分组指标 |
| 质量与响应时间难以兼顾 | 所有请求采用相同检索成本 | 根据请求特征选择合适的检索路径 |

### 核心价值

**对浏览者：** 通过持续反馈获得更贴合当前兴趣的商品列表，并减少重复或明确不感兴趣的内容。

**对运营人员：** 跟踪商品从导入到可推荐的完整状态，使新品上线、下架和异常处理可追溯。

**对算法人员：** 将候选质量、冷启动表现和推理成本放入同一评估流程，支持有依据的策略迭代。

### V1.0 产品形态

首版提供 Web 推荐体验页、商品管理页、策略对比页和效果看板。推荐服务输出默认 10 条结果，商品工作台支持批量导入与发布，效果看板展示可追溯的离线评估和演示行为数据。

交易支付、订单履约和营销投放不属于本版范围。

## 02 目标用户与核心场景

### 用户角色

| 角色 | 使用目标 | 主要入口 |
| --- | --- | --- |
| 商品浏览者 | 发现感兴趣的商品，调整推荐偏好 | 推荐首页、商品详情 |
| 商品运营人员 | 让合格新品及时进入推荐，处理上下架 | 商品工作台、批次详情 |
| 推荐策略人员 | 比较策略效果，定位人群和商品差异 | 策略对比、效果看板 |

### 场景 A 兴趣变化后的商品发现

一位用户过去主要关注游戏外设，近期连续浏览多人合作游戏。进入推荐页后，列表需要体现近期兴趣；用户收藏某款商品或标记“不感兴趣”后，下一次刷新应使用更新后的偏好与过滤条件。

体验闭环为：进入推荐页 → 浏览商品 → 查看详情或反馈 → 刷新列表。界面应让用户知道反馈已生效，同时保留撤销入口。

### 场景 B 新品进入推荐

运营人员提交一批新品资料。系统完成字段校验、内容处理和索引构建后，发布新的商品库版本。新品进入候选范围，不要求先积累足够的点击记录。

“具备推荐资格”与“实际出现在推荐列表”是两个不同状态。工作台分别显示发布状态与曝光情况，避免将入库成功误解为保证获得推荐位。

### 场景 C 策略效果评审

策略人员选择同一用户历史、商品库版本和评估时间，对比生成式、内容匹配与自适应混合策略。系统展示候选重合情况、Top 10 差异、响应耗时和评估指标，并支持查看失败样例。

### 演示身份与会话

演示环境提供“选择示例用户”和“以新用户体验”两个入口。示例用户的原始历史只读；新增反馈保存在独立演示会话中。重置会话恢复初始状态，不改写原始数据。

示例商品与人物场景仅用于说明产品行为，不代表真实用户调研结论。

## 03 产品目标与版本范围

### 产品目标

| 目标 | 衡量方式 | V1.0 验收方向 |
| --- | --- | --- |
| 推荐具有相关性 | NDCG@10、Recall@20 与同协议基线比较 | 完成可复现评估，报告整体与冷物品分组 |
| 新品能够及时参与推荐 | 发布到候选可用的时间 | 已发布且合格的新品可进入候选库 |
| 用户反馈形成体验闭环 | 反馈成功率、下一次请求的状态检查 | 收藏与屏蔽即时确认，后续请求使用新状态 |
| 推荐服务稳定可用 | 成功率、p95 延迟、回退率 | 达到第 09 节约定的演示负载目标 |
| 策略差异可以追溯 | 配置、数据、模型与商品库版本 | 任一对比结果均能定位对应运行记录 |

首版以离线推荐质量和完整体验闭环为主要验收依据。浏览点击率用于后续真实交互分析，不预设未经验证的提升幅度。

### 功能优先级

P0 为首版必需，P1 为首版增强，P2 为后续规划。

| 优先级 | 功能集合 | 版本要求 |
| --- | --- | --- |
| P0 | 推荐列表、商品详情、示例用户、反馈与重置 | 完整打通浏览与反馈闭环 |
| P0 | 商品导入、校验、发布、下架、失败重试 | 商品状态明确，发布过程可追踪 |
| P0 | 推荐策略选择、合法性过滤、异常回退 | 用户得到有效结果，异常有明确反馈 |
| P0 | 策略对比、离线指标、运行记录 | 统一输入与指标口径 |
| P1 | 细分人群分析、批次回滚、报告导出 | 提高评审与运营效率 |
| P1 | C++ 推理关键路径优化 | 正确性一致后验证性能收益 |
| P2 | 登录体系、多模态商品理解、真实流量实验 | 独立评审后进入后续版本 |

### 范围边界

首版支持商品推荐与效果分析，不包含完整电商交易系统。模型训练作为离线任务运行，前台推荐无需等待实时训练。演示反馈进入会话偏好；是否用于下一轮模型训练，由独立数据流程决定。

## 04 信息架构与交互流程

### 页面结构

| 一级页面 | 核心区域 | 主要操作 |
| --- | --- | --- |
| 推荐首页 | 用户入口、推荐流、反馈状态 | 切换用户、刷新、收藏、不感兴趣 |
| 商品详情 | 商品信息、类别、收藏状态 | 返回列表、收藏、撤销反馈 |
| 商品工作台 | 商品列表、导入批次、发布状态 | 导入、查看错误、发布、下架 |
| 策略对比 | 固定输入、策略结果、差异摘要 | 发起对比、查看明细、保存记录 |
| 效果看板 | 指标趋势、分组结果、版本信息 | 筛选、追溯、导出报告 |

### 推荐首页原型

推荐页顶部提供用户选择、历史摘要和刷新入口。主体使用商品卡片展示标题、类别与交互按钮；算法分数与候选来源收纳在可展开的“推荐详情”中。

原型示意商品为自制文本，用于说明信息层次。正式展示只使用具备可用资料的商品，图片缺失时显示统一占位图。

### 两条主要流程

**浏览者流程：** 选择用户 → 获取推荐 → 查看详情 → 收藏或屏蔽 → 刷新并继续发现。

**商品发布流程：** 提交资料 → 校验 → 构建索引 → 预览确认 → 发布生效 → 跟踪推荐情况。

刷新列表不承诺每次完全不同。对于相同历史、配置和商品库版本，结果允许保持稳定；有新反馈、新商品或版本变化时，再体现相应调整。

## 05 个性化推荐功能需求

### F01 推荐列表

进入页面后自动请求推荐，默认返回 10 条去重结果。卡片包含商品标题、类别、可用图片及收藏、屏蔽入口。推荐仅使用当前已发布且有效的商品；已屏蔽或已交互商品按统一规则过滤。

候选不足时返回实际数量，并提示“当前可推荐商品较少”；不得使用重复商品补足。标题过长截断展示，详情中可查看全文。加载期间保留页面结构，避免内容跳动。

**验收：** 列表中无重复、下架或被当前会话屏蔽的商品；相同输入能够追溯到同一配置与版本。

### F02 用户历史与冷启动

选择示例用户后展示可理解的历史摘要；新用户可选择兴趣类别，也可直接查看当前可用热门推荐。无历史时不得显示“基于你的浏览记录”等不符合事实的文案。

**验收：** 用户切换后会话状态隔离；新用户无需完成额外操作即可获得结果；重置后恢复初始历史和偏好。

### F03 商品详情与反馈

查看详情记录浏览行为；收藏采用可切换状态并即时确认；“不感兴趣”使商品从当前列表移除，并在本次会话的后续推荐中屏蔽。页面提供撤销入口，撤销后恢复推荐资格，但不保证立即进入下一次列表。

收藏与浏览更新后续推荐的历史输入，不承诺单次动作一定改变模型排序。反馈提交失败时保留可重试状态，避免界面显示成功而服务端未接收。

**验收：** 重复提交同一事件不重复计数；屏蔽在下一次请求生效；撤销与会话重置行为一致。

### F04 推荐详情与回退提示

研究详情可展示候选来源、实际策略和请求耗时。普通浏览页使用“与你近期关注的类别相关”等有明确依据的说明，不展示未经验证的心理或因果推断。

检索路径超时且存在有效回退结果时继续展示商品；详情中标记实际使用的策略。所有路径均失败时提示“暂时无法生成推荐，请稍后重试”。

## 06 新品入库与商品生命周期

### 商品状态

批次按“待校验 → 处理中 → 待发布 → 已发布”流转。任一阶段失败进入“处理失败”，保留错误原因与重试入口。商品另有“有效 / 已下架”状态，决定能否参与推荐。

### F05 批量导入

首版支持 CSV 或 JSON 文件，单批最多 1,000 条、20 MB。必填字段为商品 ID、标题与类别；描述、图片地址为可选。商品 ID 在平台内唯一，重复 ID 默认拒绝，并在错误报告中注明行号与原因。

文件校验采用整批通过后继续处理的规则。字段错误不产生部分发布结果。相同批次 ID 和相同内容的重试返回已有任务；同 ID 内容不同则要求创建新批次。

### F06 处理与发布

字段校验通过后，系统生成内容表示、语义编码与检索索引。工作台显示已处理数量、失败数量和当前阶段。预览页展示商品信息与可推荐资格，由运营人员确认发布。

发布以完整商品库版本为单位切换。发布后开始的新推荐请求使用新版本；已开始的请求完成当前版本处理。索引构建失败时继续使用此前有效版本。

### F07 下架与回滚

下架后发起的请求不得返回该商品。已打开的详情页刷新时展示“商品已下架”。版本回滚恢复兼容的模型和索引组合，当前下架名单继续生效，避免重新推荐已明确下架的商品。

### 验收条件

| 检查项 | 预期结果 |
| --- | --- |
| 新品没有历史交互 | 完成处理和发布后可参与内容候选检索 |
| 文件存在缺失字段或重复主键 | 返回完整错误报告，整批不发布 |
| 发布期间存在并发请求 | 单次请求内模型、编码与索引版本一致 |
| 处理任务失败或重复提交 | 不破坏当前服务，任务状态和重试结果明确 |
| 下架后执行版本回滚 | 下架商品仍被过滤 |

## 07 策略对比与效果看板

### F08 策略对比

支持生成式检索、内容匹配、自适应混合及选定基线。发起对比时固定用户历史、请求时间、有效商品集合和最终返回数量，展示各策略的 Top 10、共同商品、独有商品、耗时与回退状态。

对比任务单独运行并保存记录，不影响普通推荐页的默认策略。批量评估以后台任务处理，界面展示进度与取消状态。

### F09 效果看板

看板分为“离线评估”和“体验行为”两类数据。每张图表显示数据来源、时间范围、样本数与模型版本；尚未运行的评估显示“暂无评估结果”。

| 数据类别 | 核心指标 | 展示说明 |
| --- | --- | --- |
| 离线推荐效果 | NDCG@10、Recall@20 | 反映历史记录任务中的排序与命中情况 |
| 候选检索 | Recall@200、商品覆盖率 | 反映候选质量与商品触达范围 |
| 商品分组 | 常规商品、模型冷物品、低频商品 | 冷物品指训练时无交互且当前已入库的商品 |
| 服务运行 | p50 / p95、成功率、回退率 | 附硬件、并发与商品规模 |
| 体验行为 | 曝光、点击、收藏、屏蔽 | 仅统计演示或真实服务采集的行为 |

离线历史交互指标不换算成线上点击率；演示流量不用于证明真实商业收益。新品分组必须说明是“模型未训练过”还是“上线后尚无反馈”。

### F10 结果追溯

对比与评估记录包含数据版本、模型版本、商品库版本、策略配置、运行时间和结果文件。支持查看分组样例与失败原因；导出报告时同时保留指标口径和样本数。

**验收：** 相同任务可按保存配置重放；各策略使用同一评估事件集合；缺失数据不以零值替代。

## 08 算法能力与数据要求

### 推荐能力结构

用户历史用于建立兴趣表示，商品标题与类别用于建立内容表示。生成式检索预测有意义的商品语义路径；内容匹配从当前商品库中检索相近商品；路由模块根据请求特征选择检索方式，随后统一评分、过滤并返回结果。

新商品在内容处理完成后可加入检索库；模型权重更新与商品库更新分别管理。路由选择的目标是在给定响应预算下兼顾推荐相关性和候选覆盖，实际收益通过对比验证。

### 数据组成

| 数据对象 | 主要字段 | 使用要求 |
| --- | --- | --- |
| 商品 | ID、标题、类别、描述、有效状态 | 主键稳定，缺失图片允许占位，发布前完成校验 |
| 用户历史 | 匿名用户、商品、行为、发生时间 | 仅使用请求时间之前的行为 |
| 推荐请求 | 请求 ID、历史版本、策略与结果 | 保存实际执行路径和相关版本 |
| 行为事件 | 事件 ID、请求 ID、商品、类型、时间 | 可去重、可关联，演示与离线数据分离 |
| 运行记录 | 数据、模型、索引、指标与配置 | 形成可追溯的评估依据 |

### 行为口径

曝光在卡片至少 50% 可见且持续 1 秒后记录；同一请求、同一商品只计一次。若用户在达到该时长前点击，则补记相应曝光。点击率采用唯一有效点击商品对数除以唯一曝光商品对数，均按“请求 ID + 商品 ID”去重。

反馈事件关联实际返回的商品；重试使用原事件 ID。演示反馈更新会话历史，不自动改写离线训练集。实验重放的未来行为不得进入当前请求输入。

### 数据来源与评估边界

首版离线数据使用 Amazon Reviews 2023 的商品信息与历史评论交互。该数据不提供完整真实上架时间，动态库评估使用明确标注的时间代理；前台新品导入则使用系统记录的真实导入与发布时间。

技术参考包括 TIGER 的语义 ID 检索与 LIGER 的混合检索方法。模型训练、时间协议和实验细节在专项技术文档中维护。

## 09 非功能需求与验收标准

### 服务目标

以下为首版规划目标，验收时固定硬件、模型、商品库规模和请求分布，记录实际完成情况。

| 维度 | 规划目标 | 验证方法 |
| --- | --- | --- |
| 响应速度 | 推荐请求 p95 ≤ 300 ms | 模型预热后按 20 请求每秒持续测试 10 分钟 |
| 服务成功率 | 有效请求成功率 ≥ 99% | 超时与服务端错误计入失败 |
| 结果有效性 | 无重复、下架或会话屏蔽商品 | 对结果逐条执行规则检查 |
| 新品处理 | 100 条合格商品 60 秒内完成处理 | 包含内容编码与索引构建，不含人工确认等待 |
| 反馈一致性 | 已确认反馈在下一次请求生效 | 检查状态版本与后续结果 |
| 版本一致性 | 单次请求不混用不兼容版本 | 并发发布与回滚验证 |

### 异常与恢复

| 情况 | 用户可见行为 | 恢复要求 |
| --- | --- | --- |
| 推荐加载超时 | 保留已有内容，显示重试提示 | 可回退到已完成的有效候选 |
| 商品信息缺失 | 使用占位图片或缺失说明 | 不影响有效商品继续展示 |
| 反馈提交失败 | 显示未完成状态，允许重试 | 相同事件重试不重复计数 |
| 商品批次处理失败 | 展示失败阶段与错误报告 | 旧商品库继续提供推荐 |
| 当前没有有效候选 | 展示空状态与偏好调整入口 | 不返回虚构或重复商品 |

### 访问与兼容要求

普通浏览者只访问推荐体验；商品发布、下架与策略管理入口需要管理员权限，演示环境可通过本地受限访问实现。导入内容以文本安全展示，避免将商品标题或描述作为页面代码执行。

桌面端优先适配 1280 px 及以上宽度，移动端采用单列商品卡片。按钮应有明确状态，键盘可完成主要操作，颜色不作为区分状态的唯一方式。

## 10 版本计划与交付验收

### 版本推进

| 阶段 | 建设内容 | 阶段出口 |
| --- | --- | --- |
| V0.1 推荐体验 | 用户历史、基础推荐、详情和反馈 | 完成浏览与反馈闭环 |
| V0.2 动态商品库 | 商品导入、校验、内容处理、发布与下架 | 新品能够进入有效候选范围 |
| V0.3 推荐策略 | 生成式、内容匹配、混合检索与统一排序 | 多策略在相同输入下可比较 |
| V0.4 效果管理 | 统一评估、分组看板、运行记录 | 效果与成本能够追溯 |
| V1.0 展示版本 | 联调、性能验证、异常恢复与文档 | 完成端到端验收与演示 |

### 发布前验收

1. 浏览者能够完成选择用户、获取推荐、查看详情、反馈和重置会话。
2. 运营人员能够完成商品导入、错误修正、发布、下架和状态查询。
3. 同一历史和商品库下可以对比推荐策略，并查看对应指标和运行记录。
4. 版本切换与异常回退不产生非法商品，不丢失已经确认的会话反馈。
5. 性能结果附带运行环境与负载信息，所有展示指标均可关联到真实记录。

### 交付内容

交付 Web 演示入口、推荐服务、商品处理流程、离线评估报告、接口说明、部署说明与验收记录。展示版本提供可重复的示例用户和自制新品批次，能够完整演示“兴趣输入 → 商品推荐 → 用户反馈 → 新品发布 → 效果比较”。

### 项目依赖

推荐页面依赖可用商品资料与服务接口；新品发布依赖兼容的内容编码和索引；效果看板依赖统一评估产物。算法质量、数据完整性或算力不足时，通过阶段评审调整发布范围，并在版本说明中记录。

### 参考资料

- [Amazon Reviews 2023 数据说明](https://amazon-reviews-2023.github.io/main.html)
- [TIGER 生成式检索论文](https://arxiv.org/abs/2305.05065)
- [LIGER 官方项目](https://github.com/facebookresearch/liger)

本版定义产品需求与验收标准。模型结构、训练设置和统计检验方案另见项目技术与实验文档。
