{
  "checked_at": "2026-09-23",
  "environment": {
    "database": "local PostgreSQL 18.6",
    "platform": "Windows",
    "python": "3.12"
  },
  "commands": [
    ".\\.venv\\Scripts\\python.exe -m pytest -p no:cacheprovider --basetemp=tmp\\pytest-bundle tests/test_bundle_validation.py",
    ".\\.venv-research\\Scripts\\python.exe -m pytest -p no:cacheprovider --basetemp=tmp\\pytest-m21-research --junitxml=docs\\validation\\m21-bundle-tests.xml",
    ".\\.venv\\Scripts\\python.exe -m pytest -p no:cacheprovider --basetemp=tmp\\pytest-m21-postgres tests\\test_postgres_api.py"
  ],
  "results": {
    "bundle_validation": "8 passed",
    "full_regression": "176 passed, 1 PostgreSQL integration test skipped because the database URL was intentionally absent from that process",
    "postgres_api_integration": "1 passed after loading the ignored local database URL"
  },
  "verified_behaviors": [
    "only canonical bundle UUID directories beneath the managed root are accepted",
    "manifest objects use an exact versioned schema and timezone-aware timestamps",
    "listed files must be regular files with matching byte counts and SHA-256 digests",
    "the bundle file set is closed and path traversal or symbolic links are rejected",
    "item mappings use contiguous internal IDs, unique external IDs, and a matching ordered-set fingerprint",
    "embedding bytes match item count, dimension, and float16 or float32 element size",
    "strategy budgets and artifact role references are internally compatible",
    "the command emits structured success or failure output"
  ],
  "boundaries": [
    "model and index payloads are not deserialized",
    "runtime dependency compatibility and sample inference consistency are not checked",
    "validation does not write publication state, activate a bundle, or change readiness",
    "the tests use synthetic bundle files and do not validate a trained production bundle"
  ]
}
