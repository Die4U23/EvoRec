# EvoRec 面向推广搜实习的优化与提高方向

编写日期：2026-09-16。依据：项目当前全部文档、五轮实验结果（R01–R05）、`src/evorec/research/` 代码以及对 `datasets/video_games_r05.csv` 的两项复核统计。

## 一句话结论

这个项目在**实验严谨性**上已经远超一般学生项目（封闭测试集、预登记协议、多种子、逐请求轨迹、独立审计），这是面试时最独特的资本；但从推广搜（推荐 / 广告 / 搜索）算法实习的面试官视角看，它有三个明显短板：**头条指标口径把自己的成果稀释了**、**序列模型输给统计基线这件事没有被修复**、**缺少推广搜岗位的"标配"技术栈**（ID embedding 精排、负采样双塔、ANN、CTR/AUC、多目标、校准、搜索相关性）。下面按"先修叙事，再补标配，最后按方向延伸"的顺序给出建议。

---

## 1. 先弄清面试官会怎么看这个项目

推广搜算法实习面试通常分三块：机器学习与推荐基础八股、项目深挖、手撕代码。项目深挖的套路是"为什么这么做 → 结果为什么是这样 → 怎么证明它对 → 有什么问题、怎么改"。

对照这个套路，EvoRec 现在的状态：

| 维度 | 现状 | 面试官的第一反应 |
| --- | --- | --- |
| 实验协议 | 全局时间切分、封闭测试、验证选型、三种子、审计哈希 | 非常加分，几乎没有学生做到这一步 |
| 链路完整度 | ItemCF / 时间衰减热门 → SASRec → 内容双塔 → RRF 融合 → 规则门控 → listwise 精排 | 能对应到"多路召回 → 融合 → 精排 → 重排"，加分 |
| 头条数字 | NDCG@10 ≈ 0.009，"+39.5%" 是相对 RRF | 0.009 会立刻被追问"为什么这么低"；RRF 在有历史请求上比 CF-blend 还差，会被质疑对比对象太弱 |
| 神经序列模型 | SASRec 三种子 0.0056，输给 CF-blend 0.0084 | 会被追问"为什么 Transformer 输给 ItemCF，你查过原因吗、修了吗" |
| 冷启动 | 冷目标 Top20 命中 26 / 7,088，候选池只覆盖 127 个 | 方向正确，但会被问"瓶颈在召回，你为什么在排序上花功夫" |
| 岗位标配 | 无 ID embedding 精排、无 in-batch 负采样、无 ANN、无 AUC/GAUC、无多目标、无校准、无搜索 | 会被问到，答不上来则项目再严谨也补不回来 |
| 工程呈现 | 只有 1 个 git 提交，R01–R05 全部未提交；研究代码极度压缩（`a=b;c=d` 风格） | 打开仓库的第一印象会打折 |

所以优先级不是"再加一个模型"，而是先把已经做出来的东西讲对、讲清，再补齐岗位必需的技术点。

---

## 2. 第一优先级（约两周）：把现有成果讲对

### 2.1 修正头条指标口径

问题：`docs/experiments/r05-ranker/results.json` 的 `cohorts` 里已经有 `history_present` / `cold_user` / `model_cold_available` 分组，但 README、博客和报告的头条都用 `all_positive_events`。测试集 12,720 个请求里 7,506 个没有历史，这部分所有方法都退化为 `RecentPopular`，分数完全相同（0.0085）。也就是说头条数字里有 59% 是常数。

同一份测试结果按分组重新读：

| 方法（R05 测试，同候选池） | 全部请求 NDCG@10 | 有历史请求 NDCG@10（n=5,214） | 相对 CF-blend |
| --- | ---: | ---: | ---: |
| CF-blend（最强统计基线） | 0.0072 | 0.0053 | — |
| RRF（固定融合） | 0.0065 | 0.0037 | −30% |
| ListMLP-s17 | 0.0094 | 0.0108 | +104% |
| ColdListMLP-s17 | 0.0091 | 0.0100 | +89% |

要做的事：

1. README、博客第八节、R05 报告的头条改为"有历史请求"分组，同时保留全部请求的数字并说明分母构成。相对提升以最强基线 CF-blend 为参照，而不是 RRF——面试官一眼就能看出 RRF 在有历史请求上比 CF-blend 还差 30%，"+39.5%"会被认为是打稻草人。
2. 在报告中明确回答"为什么绝对值这么低"：0-core 数据、73% 用户只有 1 条交互、全局时间切分、全部正反馈进分母、目标含 56% 训练未见商品。这段话要能在面试里 30 秒说完。
3. 补一个**标准协议对照**：在同一类别上跑一次 5-core 过滤 + leave-one-out + 全量排序的 SASRec / ItemCF，得到一个和公开论文可比的数字。这不是为了替代现有协议，而是回答"你的指标和论文对得上吗"这个必问题。可以做成 `research/configs/r06-standard-protocol.json`，复用 `protocol.py` 和 `evaluation.py`。

### 2.2 补显著性检验（成本最低、收益最高）

STATUS 和报告里至少出现了 6 次"尚未进行显著性检验"。逐请求轨迹（`test-*.jsonl.gz`）里每条都有 `metrics`，做**配对 bootstrap** 或**配对 t 检验**只需要几十行代码：

- 对同一批 query，两方法的 NDCG@10 逐条相减，bootstrap 重采样 query 得到差值的 95% 置信区间。
- 输出到 `docs/validation/r05-significance.json`，并写进报告。
- 顺带对 `history_present` 分组单独做一次。

做完后就能把所有"观测差值"改成"差值 X，95% CI [a, b]"，这是面试中"你怎么证明改进是真的"的标准答案。建议放在 `scripts/paired_bootstrap.py`。

### 2.3 把"SASRec 输给统计基线"变成诊断故事

现状（R02）：54,060 个训练样例、词表 30,394、hidden 64、两层，三种子 NDCG 0.0056 ± 0.0002，输给 CF-blend 0.0084。原因几乎可以肯定是数据量：1/20 用户采样后，绝大多数用户序列长度 ≤ 2，Transformer 没有可学的序列结构。

建议按顺序做：

1. **扩大到全量用户**。完整文件 4,555,500 条交互，RTX 4060 8 GB 跑 64 维 SASRec 完全够（当前训练峰值显存不到 1 GB）。`sample.py` 的 `denominator` 改成 1 即可，注意要登记新协议、新的用户桶，不能复用已看过测试集的样本。
2. **5-core 过滤后再训练**，对照 2.1 的标准协议。
3. 补 SASRec 常规做法：dropout / 学习率网格、负采样版本（sampled softmax）对比全词表 CE。

目标不是刷分，而是让简历上能写"定位到序列模型失败原因是采样过稀，扩大数据后在有历史请求上超过统计基线 X%"。这句话在面试里比"我训练了 SASRec"值钱十倍。

### 2.4 仓库第一印象

- **git 提交历史**：现在只有 1 个提交，`docs/experiments/` 整个目录未跟踪。按 R01 → R02 → R03 → R04 → R05 分批提交，每个提交信息写清做了什么。面试官会看提交历史判断是不是自己一步步做的。
- **研究代码可读性**：`train_ranker.py`、`ranker_data.py`、`gating.py` 大量 `a=b;c=d`、无空格、单字母变量。用 `ruff format` 或 `black` 统一格式，给核心函数补一句 docstring。历史运行目录里的 `source/` 快照是拷贝件，重新格式化当前源码不会破坏审计哈希。
- **README 重构**：现在开头是三段带大量限定语的数字，30 秒内看不出项目做了什么。建议结构：一段话介绍 → 一张链路图（召回 / 融合 / 精排 / 门控）→ 一张分组结果表（2.1 那张）→ 三条核心发现 → 复现命令 → 文档索引。所有限定语保留在报告里，README 只留一句"详细边界见报告"。
- **环境**：`.venv` + `.venv-research`（system-site-packages）两套环境，外人很难复现。合并成一份 `requirements.txt`（或 `uv.lock`），研究依赖用 extras；Docker 可以不做。
- **清理 `tmp/`**：十几个一次性脚本要么删掉，要么并入 `scripts/`。

### 2.5 简历与面试话术

把项目翻译成推广搜术语。示例：

> **EvoRec：动态商品库推荐系统（Amazon Reviews 2023 Video Games，450 万交互）**
> - 搭建严格时间协议的离线评估框架：全局时间切分、封闭测试集、验证集选型、三种子统计、逐请求轨迹与独立审计脚本，杜绝目标泄漏与测试集调参。
> - 多路召回：ItemCF + 时间衰减热门融合、TF-IDF/SVD 内容双塔（训练集限定拟合），RRF 融合；针对训练未见冷商品建立内容召回入口。
> - 精排：实现 ID-free 残差 listwise MLP（520→128→64→1，候选内 softmax 交叉熵，冷商品加权），在有历史请求上 NDCG@10 相对最强统计基线 +104%（配对 bootstrap 95% CI …）。
> - 冷启动：设计时间滚动的模拟冷商品训练任务、保量 / 门控重排消融与失败定位分析，定位候选可达性为主要瓶颈（冷目标入池率 1.8%）。

面试中必然被追问的问题（都从项目里长出来的，提前准备）：

1. 为什么 NDCG 绝对值这么低？分母是怎么构成的？
2. 为什么 SASRec 输给 ItemCF？你怎么诊断的、怎么修的？
3. 为什么用 RRF 而不是分数加权？RRF 的常数 60 怎么选的？
4. listwise / pairwise / pointwise 损失各自的适用场景；你为什么选 softmax 交叉熵；未观测候选当负例有什么问题？
5. 为什么不用 ID embedding？冷启动之外，ID-free 会损失什么？
6. 双塔为什么用全词表 softmax 而不是 in-batch 负采样？in-batch 的热门偏差怎么修（log-Q 校正）？
7. 时间切分下怎么防泄漏？你发现过哪些泄漏点（编码器拟合范围、商品可用时间代理、同时间戳批次）？
8. 冷商品推荐的瓶颈在召回还是排序？你有什么证据？
9. 如果要上线 20 QPS、p95 300 ms，链路怎么设计？哪部分最慢？
10. 离线 NDCG 提升为什么不能直接说线上 CTR 会提升？

---

## 3. 第二优先级（一到两个月）：补齐推广搜"标配"

这些是三个方向面试都会问的技术点，项目里目前没有。每一项都可以复用现有协议与评估框架，作为 R06 及之后的轮次登记。

### 3.1 召回侧

**（a）in-batch 负采样双塔 + log-Q 校正。** 现在 `ContentTower.forward` 对全部约 3 万训练商品做 softmax。工业双塔标准做法是 batch 内互为负例并用采样概率校正热门偏差。实现成本一天，面试必问。对照实验：全词表 CE vs in-batch vs in-batch + log-Q，看 Recall@200 和冷商品召回。

**（b）ANN 检索与精确检索对照。** `ContentPredictor.rank_many` 是精确矩阵乘。加 Faiss（IVF / HNSW）后对比召回率损失与延迟，这是 `docs/05-research-protocol.md` 第 4 节自己写下的要求。你的主语言是 C++，可以自己写一个 HNSW 或 IVF-Flat 用 pybind11 接进来，和 Faiss 做正确性与速度对照——这是 C++ 背景在推广搜岗位上最自然的差异化点，前提是要有测量数字。

**（c）预训练文本编码器替换 TF-IDF/SVD。** 冷目标入池率只有 127 / 7,088（1.8%），排序再强也没用。用 `bge-small-zh/en` 或 `all-MiniLM-L6-v2` 编码标题 + 类别 + `features` / `description`（元数据里有这些字段，目前只用了 title 和 categories），冻结向量后重跑 R03 双塔。预期冷商品召回会有数量级变化。这一步能顺便回答"为什么不用预训练模型"。

**（d）冷用户召回。** 59% 请求无历史，目前退化为热门。哪怕加一个"请求时刻类别热度"或"近期上架商品"的入口，也能把 `cold_user` 分组从常数变成可比较的对象。

### 3.2 精排侧

**（a）标准 ID-based CTR 模型作为对照。** 加 user ID / item ID / 类别 embedding，实现 DCN-v2 或 DIN（用户历史对候选做 attention），和现有 ID-free ListMLP 在同一候选池上对比。面试问"你了解哪些精排模型"时要有自己跑过的。

**（b）pointwise BCE + AUC / GAUC。** 广告方向面试从 AUC 开始问：AUC 的概率解释、GAUC 为什么要按用户分组、AUC 和 NDCG 的关系。把候选池样本改成 pointwise 标签跑一次 BCE，报告 AUC、GAUC、NDCG，比较 listwise 和 pointwise 的差异。

**（c）校准。** 广告排序用 pCTR × bid，pCTR 必须校准。对 pointwise 模型输出做 Platt scaling / isotonic，画可靠性曲线，报告 ECE。这是广告岗位区别于推荐岗位的核心问题。

### 3.3 多目标

用现有数据可以构造两个任务：rating ≥ 4（正反馈）与是否留下评论（元数据里 `rating_number` 或原始 review 文件）。实现一个 ESMM（CTR → CTCVR）或 MMoE 简版，讨论样本选择偏差。哪怕规模小，能讲清 ESMM 为什么在全空间建模就够了。

### 3.4 在线闭环（M12）

把 `artifacts/runs/r05-ranker-20260916/ColdListMLP-s17/best.pt` 接进 FastAPI 的 `/api/v1/recommend`：加载编码器 → 召回 → 特征 → 精排 → 合法性过滤。然后用 `locust` 或 `hey` 按 PRD 第 09 节的目标（20 QPS、p95 ≤ 300 ms）压一次，记录各阶段耗时。这一步不需要数据库和前端，一两天能完成，但能让你在面试里讲"链路每一段耗时多少、瓶颈在哪、怎么优化"，也是 C++ 优化（O01）的前置测量。

---

## 4. 第三优先级：按目标方向延伸

三个方向的侧重点不同，建议根据投递目标二选一深入，不要同时铺开。

### 推荐方向

现有项目已经是推荐项目，补齐第 3 节即可。可以再加：重排多样性（MMR / DPP）、序列模型的对比（SASRec vs BERT4Rec vs GRU4Rec）、离线 / 在线不一致的讨论（曝光偏差、IPS）。

### 广告方向

除了 3.2 的 AUC / 校准 / 多目标，建议用一个标准 CTR 数据集单独做一个小项目：Criteo 或 Avazu 跑 LR → FM → DeepFM → DCN-v2，报告 AUC / logloss，做特征哈希、embedding 维度与正则的消融。广告面试还会问 eCPM 排序、二价拍卖、预算平滑（pacing）、延迟反馈，这些是知识点，不需要项目，但要会讲。

### 搜索方向

项目里"搜"完全空缺。推荐用 **Amazon ESCI（Shopping Queries Dataset，KDD Cup 2022）**做 query–product 相关性：BM25 召回 → 双塔语义召回 → cross-encoder 精排，报告 NDCG / MRR，做 pointwise / pairwise / LambdaMART 对比。这套数据也是 Amazon 商品，可以复用你的评估框架和内容编码代码。搜索面试的额外知识点：query 理解（分词、纠错、改写、意图）、倒排索引、相关性与点击率的权衡。

---

## 5. 不建议继续投入的方向

- **在 1/20 采样上继续调 R05 排序器结构。** 候选池覆盖只有 1.8%，排序侧的上限已经被召回锁死。
- **先做前端、数据库、Docker。** 这些对算法实习加分很少；M12 的最小接口接入（3.4）足够证明工程能力。
- **完整复现 TIGER / LIGER 生成式检索。** 成本高、面试不是必问；了解语义 ID 的思想并能说清优缺点即可。
- **继续加长文档。** 现有文档已经很完整，问题是太密。之后每个新轮次的报告控制在一页，限定语集中放在末尾。

---

## 6. 执行清单

| 序号 | 事项 | 预计投入 | 产出 | 对应第几节 |
| --- | --- | --- | --- | --- |
| 1 | 按 R01–R05 分批提交 git，跟踪 `docs/experiments/` | 半天 | 可信的提交历史 | 2.4 |
| 2 | 配对 bootstrap 脚本，补 R03 / R05 置信区间 | 1 天 | `scripts/paired_bootstrap.py`、报告更新 | 2.2 |
| 3 | 头条指标改为有历史分组，参照改为 CF-blend | 半天 | README / 博客 / R05 报告 | 2.1 |
| 4 | 5-core + leave-one-out 标准协议对照 | 2 天 | R06 报告，可与论文对照的数字 | 2.1 |
| 5 | 全量用户重训 SASRec，写诊断 | 3 天 | R07 报告，"为什么输、怎么修"的故事 | 2.3 |
| 6 | 研究代码格式化、README 重构、合并环境 | 1–2 天 | 仓库第一印象 | 2.4 |
| 7 | 简历项目描述与十个追问的答案 | 1 天 | 面试准备文档 | 2.5 |
| 8 | in-batch 负采样双塔 + log-Q | 2 天 | R08 | 3.1a |
| 9 | 预训练文本编码器替换 SVD | 2 天 | 冷商品召回数量级变化 | 3.1c |
| 10 | Faiss / 自写 HNSW 与精确检索对照 | 3–5 天 | 召回率损失与延迟表；C++ 差异化 | 3.1b |
| 11 | DCN-v2 / DIN + AUC / GAUC + 校准 | 4–5 天 | 精排对照与广告知识点 | 3.2 |
| 12 | 接口接入 ColdListMLP，压测 p95 | 2 天 | 链路耗时分解 | 3.4 |
| 13 | 按方向：Criteo CTR 或 ESCI 搜索小项目 | 1–2 周 | 第二个项目 | 4 |

前 7 项在两周内可以完成，完成后就可以开始投递日常实习；8–12 项边投递边做；13 项按目标方向选做。

---

## 7. 保留的优点（不要在重构中丢掉）

- 封闭测试集与验证选型的纪律，包括"看过测试集后不回改"的记录。
- 训练集限定拟合（IDF / SVD 只用训练商品）、时间滚动模拟冷商品、同时间戳批次不互相可见——这些是真正的防泄漏细节，面试里说出来会被认可。
- 逐请求轨迹与独立审计脚本。
- 失败定位分析（R04 error-analysis）。
- 持续更新的项目实录。建议精简后公开（知乎 / GitHub Pages），公开写作对推广搜团队是明确的加分项。

这些是项目的护城河；第 2–4 节的建议都是在这个基础上把"讲对"和"补齐"做完。
