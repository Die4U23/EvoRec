# C++ 扩展边界

预定采用 C++20、CMake 与 pybind11；当前没有 C++ 实现或性能结论。

只有 O01 性能分析确认收益空间后，才选择前缀约束查询、批量候选处理或索引生命周期模块。先定义批量输入输出、所有权、并发与并列分数规则，再与现有实现比较。

验收需要正确性、峰值内存、模块耗时和端到端耗时证据。Faiss 集成不作为自研 C++ 成果。收益不足时保留简单实现并记录原因。
