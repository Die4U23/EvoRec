# EvoRec 动态物品库生成式推荐系统

产品需求文档与研究实验方案

版本 1.0　｜　2026 年 9 月 14 日　｜　面向推荐算法实习作品集

**文档状态：方案已完成，项目尚未实现，所有效果和性能数值均为拟定目标。**

## 01 项目决策

推荐选择 EvoRec，研究新商品持续加入时，生成式推荐的候选覆盖、冷启动表现，以及生成式与稠密检索之间的计算分配。最终作品是一套可复现实验、一项有明确边界的算法改进，以及能展示新物品加入过程的推荐演示系统。

项目的核心问题是：**在相同的候选数量和推理预算下，能否根据请求的历史信息和当前物品库状态，选择更合适的检索策略，使新物品更容易被找到，同时保持整体推荐质量？**

算法主体使用 Python 与 PyTorch；C++ 用于后期的前缀树、索引快照和推理服务优化。研究优先级高于网站功能与服务规模。项目按研究阶段验收，完整周期参考第 14 节。

这是针对推荐算法实习的选题判断，无法保证录用、论文发表或领先所有候选人。它的竞争力取决于是否真正完成强基线、无泄漏评估、有效改进与独立复现，而不是项目名称或模型参数规模。

### 为什么选择这个问题

生成式推荐以序列建模方式预测推荐结果。TIGER 研究用语义 ID 表示物品并生成推荐；LIGER 已研究生成式与稠密检索的结合。OpenOneRec 则公开了更大规模的推荐基础模型与训练框架。这些工作说明该方向既有研究价值，也有可参考的实现。[TIGER 论文](https://arxiv.org/abs/2305.05065)；[LIGER 官方实现](https://github.com/facebookresearch/liger)；[OpenOneRec 官方项目](https://github.com/Kuaishou-OneRec/OpenOneRec)。

但共享语义 token 并不等于模型必然会推荐新商品。2026 年的一项预印本从时间切分、未见 token 和语义路径等角度讨论了这一限制。本项目据此选择可检验的问题，而不预先宣称解决了冷启动。[时间视角下的语义 ID 冷启动研究](https://arxiv.org/abs/2607.21101)。

### 与岗位的关系

推荐算法实习需要用户行为建模、机器学习基础、模型训练和实验分析能力。公开的百度推荐算法实习说明也涉及兴趣建模、序列推荐和排序优化。EvoRec 能展示这些能力中的一部分；数学基础、论文理解和编程能力仍需要单独准备。[百度推荐算法实习岗位说明](https://talent.baidu.com/jobs/detail/INTERN/f7769b9b-2597-4d5f-a7a7-88705d38cf2a)。

## 02 产品定位与用户

产品形态是“推荐实验工作台 + 动态商品推荐演示”。研究者可以重放历史请求、比较策略和分析失败原因；浏览者可以查看推荐列表，并观察商品库更新前后推荐结果的变化。

| 用户 | 核心需求 | 完成标志 |
| --- | --- | --- |
| 推荐算法研究者 | 用同一协议公平比较多种模型 | 一条实验配置生成可追溯的结果表 |
| 推荐业务开发者 | 新商品加入后能参与推荐 | 新快照发布后，合格商品进入候选库 |
| 演示系统浏览者 | 得到与历史兴趣相关的商品 | 展示真实可用商品，支持新用户回退 |
| 面试官或复现者 | 判断工作是否可信且由作者理解 | 能复现关键表格，并追问设计与失败案例 |

业务场景采用电商商品发现。用户可能长期购买某类商品，近期开始关注另一主题；平台同时不断添加新商品。系统需要从当前有效商品库中生成 Top 10 推荐。

第一版不要求交易、支付、商家管理或真实流量。目标是研究推荐效果与计算成本，不以业务功能数量评判完成度。

## 03 范围与最终交付

### 必须完成的研究范围

1. 构建可追踪物品加入时间的离线协议，明确数据能支持与不能支持的结论。
2. 复现流行度、ItemCF、SASRec-CE、内容感知稠密检索、TIGER 与 LIGER。
3. 分析语义 token、前缀支持、碰撞、解码截断与冷启动失效的关系。
4. 提出并检验预算约束下的请求级检索路由；与最强固定策略公平比较。
5. 完成至少两个商品领域、三个随机种子的主实验和关键消融。
6. 提供动态商品库演示、实验报告、复现入口和贡献说明。

### 有条件加入的扩展

HSTU 用于补充更强的序列建模对照；OpenOneRec 用于工业架构学习或单独的迁移实验。二者不等同于 TIGER 的语义 ID 自回归检索，必须说明任务与候选口径的区别。[Meta 官方项目](https://github.com/meta-recsys/generative-recommenders)。

图像特征、强化学习后训练、跨领域迁移、大模型指令推荐均属于后续独立研究，不同时进入首轮主实验。扩展必须回答新增问题，不能只增加技术名词。

### 交付清单

| 编号 | 交付物 | 内容 |
| --- | --- | --- |
| D01 | 项目仓库 | 环境锁定、数据脚本、模型、评估、测试、运行入口 |
| D02 | 数据卡与协议 | 切分时间、过滤量、字段可用性、物品库定义 |
| D03 | 基线复现报告 | 官方结果复现与项目协议结果分表呈现 |
| D04 | 算法研究报告 | 假设、方法、消融、置信区间、失败案例 |
| D05 | 演示系统 | 用户历史、策略对比、新商品加入与快照回滚 |
| D06 | 推理性能报告 | 硬件、延迟、吞吐、显存、候选预算与测量边界 |
| D07 | 面试材料 | 架构图、三分钟演示、贡献清单、简历实测表述 |

## 04 研究假设与个人贡献

### H1 语义路径支持与冷物品表现存在关系

待检验：在控制物品训练频次、内容质量与用户历史长度后，训练中较少出现的语义前缀是否仍与较低的冷物品召回相关。先做分组诊断和 Oracle 前缀实验，再决定是否需要改模型。Oracle 实验向诊断程序提供目标的正确前缀，只用于定位错误，绝不计入可部署方法的效果。

严格区分“理论不可达”和“概率很低”。物品不在当前库、ID 不可表示或被硬约束排除，才属于结构上的不可达；某个前缀从未训练过，并不自动意味着它在数学上无法生成。

### H2 按请求分配检索预算优于固定混合

待检验：只使用请求到达时可见的历史与物品库信息，选择稠密检索、生成式检索或二者混合，是否能改善推荐效果与计算消耗之间的关系。主要对手是经过调参的固定混合策略与 LIGER，而非只有热门推荐。

### H3 路由收益能跨领域和更新阶段保持

待检验：第一领域确定的方法，在第二领域按预先约定的有限调参预算下能否保持趋势；模型冻结期间，随着物品库更新，收益是否会消失。不能只挑选一个有利数据集、一个种子或一段时间。

### 贡献边界

“生成式 + 稠密检索”已有相关工作，本项目不将其视为首创。个人贡献候选是：动态物品库评估协议、可达性诊断实现、预算路由方法、严谨的比较结果，以及这些结论对应的代码。是否构成论文创新，需要更全面的相关工作审查与实际实验支持。

如果简单固定策略与复杂路由一样好，报告应保留该结论，并选择简单策略部署；不要为保留“创新点”而弱化基线。

## 05 数据选择与字段合同

### 主数据

使用 Amazon Reviews 2023，首个领域为 Video_Games，第二个领域为 Toys_and_Games。第三个领域作为后续外部验证，由训练阶段的数据量与完整性确定，不能按测试集收益选择。原始数据包含评论行为、时间戳与商品元数据；商品关联使用 parent_asin。[数据官方说明](https://amazon-reviews-2023.github.io/main.html)。

使用原始数据或官方 0-core 交互作为入口。不能只用全量 5-core 过滤后的数据研究冷启动，因为这会改变低频物品与用户的分布。论文复现轨允许遵循原论文过滤方式，但须单独标记。

这里预测的是“下一次被记录的商品交互”。评论时间不是准确购买时间，未交互也不等于不喜欢；因此主结果不能写成点击率、转化率或真实购买提升。

| 字段 | 用途 | 时间和使用约束 |
| --- | --- | --- |
| user_id | 构造历史与用户级统计 | 映射后保存，不用于身份识别 |
| parent_asin | 商品主键 | 同一商品跨快照保持稳定 |
| timestamp | 排序与切分 | 明确单位，严格排除未来行为 |
| rating | 辅助分组分析 | 主任务不把低评分记录直接删成“无行为” |
| 商品标题与类别 | 内容编码与展示 | 静态元数据快照，注明历史可得性限制 |
| 商品描述与特征 | 扩展内容表示 | 首轮只用标题与类别，控制变量后加入 |
| 评论正文与评论标题 | 不进入主模型输入 | 目标评论包含事后信息，存在泄漏风险 |
| 总评分数与平均评分 | 禁止进入主特征 | 数据抓取时统计可能包含未来信息 |

主任务用全部有效评论记录表示被观测交互，去重后保留同一用户与商品的最早一次交互。若研究评分大于等于 4 的偏好任务，必须新建任务版本并重跑全部对照，不能混用指标。

### 数据处理要求

流式读取，校验主键、时间范围、空文本与重复行。每一步记录输入量、输出量和剔除原因。用户活跃阈值、词表、频次、归一化参数与码本学习仅依赖训练可见数据。

保留缺少元数据的商品，使用固定缺失内容表示并单独统计。纯内容模型可能无法有效区分这些商品，这属于实际限制；主结果不能通过仅保留有文本的样本来掩盖它。

模型训练、数据和第三方代码的许可分别记录。发布自己的代码与处理脚本，原始数据按官方渠道获取；不能把下载到的数据默认当作可以整包重新分发的素材。

## 06 时间协议与动态物品库

### 两条轨道分别报告

**R 轨为论文复现。** 遵循 TIGER 或 LIGER 原始协议，记录数据版本、过滤、候选集和评估实现。目标是验证实现，不将该轨结果直接解释为动态上线效果。

**D 轨为项目主实验。** 按全局时间切分并冻结模型，重放物品库更新与用户历史变化。默认使用 Amazon 官方 timestamp 切分所提供的两个时间点，具体值与单位必须在下载的官方版本中校验并写入 manifest；不得静默替换为逐用户留一法。[官方切分脚本说明](https://github.com/hyp1231/AmazonReviews2023/blob/main/benchmark_scripts/README.md)。

训练区间为 t < T1，验证区间为 T1 ≤ t < T2，测试区间为 t ≥ T2。模型只用训练区间拟合；验证用于参数选择，测试在方案冻结后运行。相同时间戳的事件作为一组处理，不互相提供历史。

### 物品加入时间的实际限制

该数据不提供完整、可靠的历史上架记录。D 轨采用“第一次观测到交互的时间”作为物品可见时间代理：处理时间 t 的请求时，仅允许 first_seen < t 的物品进入候选库。当前请求的目标不能因评估器知道答案而提前插入。

这是一种保守的动态物品库代理，**不等同真实上架历史**。商品首次出现的请求可能无法被推荐，应同时报告“全事件结果”和“目标已入库事件结果”，以及不可入库目标比例。所有方法使用同一可用商品集。

静态商品文本也不是历史版本。本项目因此只能声称在“时间隔离的交互 + 静态内容快照 + 首次观测入库代理”上评估，不能声称完成完全历史无泄漏的线上复现。需要验证文本预训练模型是否可能接触数据，并报告其版本、发布时间及未知之处。

### 冷启动的精确定义

| 分组 | 定义 | 可以说明什么 |
| --- | --- | --- |
| Warm | T1 前至少有一次训练交互 | 模型有机会学习该商品的协同信号 |
| Model-cold | T1 前零训练交互，当前已入库 | 模型未用该商品交互训练，但物品库已可见 |
| Head 与 Tail | 按训练频次划分并冻结阈值 | 检查流行度偏差，不能用测试频次分组 |
| 首次出现目标 | first_seen 等于当前时间 | 本代理协议下尚不可推荐，计入全事件失败 |
| 零反馈上架模拟 | 人工设置元数据先于行为入库 | 仅说明受控模拟，不证明真实零反馈效果 |

Model-cold 不等于当前从未有任何反馈：测试期较早的交互可能已使它入库。主文必须保持这一差别。真正零反馈新商品的研究作为独立模拟附录，公开上架时刻生成规则与随机种子。

### 历史与训练的隔离

评估时允许把已经发生的早期验证或测试交互更新到用户历史，这是预先声明的顺序重放；不据此更新模型权重、训练统计或码本。预测当前请求后才更新历史。离线真值只交给评估器，服务接口中不存在 target_item_id。

同一用户历史只保留过去的有效事件；输入序列最多取最近 100 条，已交互过滤使用完整可见历史。新用户使用内容或热门回退，并单独报告。

## 07 算法基线与公平比较

| 基线 | 作用 | 必须锁定的公平条件 |
| --- | --- | --- |
| Popularity | 检查频次是否已解释主要收益 | 仅训练频次，动态库过滤一致 |
| ItemCF | 检查简单协同是否足够 | 训练交互范围一致，不使用未来共现 |
| SASRec-CE | 强序列 ID 基线 | 完整 softmax 或清楚标注近似损失 |
| 内容感知稠密检索 | 冷物品可泛化的主要对手 | 内容编码器、历史与候选库一致 |
| TIGER | 语义 ID 生成式基线 | 码本、解码预算与碰撞处理清楚可查 |
| LIGER | 最接近的混合检索工作 | 官方复现与动态适配结果分开 |
| 固定混合与规则路由 | 检验学习路由是否必要 | 相同候选上限和预算调参机会 |

SASRec 不能仅用一个未经调参的弱实现；建议以全交叉熵版本为主，并明确它与原论文训练方式的区别。相同模型名称不代表相同训练协议。[SASRec 论文](https://arxiv.org/abs/1808.09781)；[序列推荐时间切分研究](https://doi.org/10.1145/3705328.3748164)。

内容感知稠密检索采用“用户序列编码器 + 商品内容向量投影”，训练用户与下一交互商品的匹配。以精确全库打分为质量参照，ANN 结果另外报告相对精确检索的召回损失。可复用 Faiss 构建索引。[Faiss 官方文档](https://github.com/facebookresearch/faiss/wiki)。

所有模型使用相同的训练标签范围。首轮为每个可训练模型分配相同数量的有效调参试验，同时记录 GPU 小时；报告中同时给出等调参预算和等推理预算视角，不能通过额外训练或更多候选制造收益。

## 08 方法设计

### 8.1 商品内容与语义 ID

固定文本编码器版本和输入模板，将标题与类别编码为内容向量。第一轮复现优先采用基线配置；任何编码器替换均作为新实验，不能把更强的文本模型带来的收益算给路由器。

RQ-VAE 将连续向量编码为多个离散 token，类似给商品分配一串共享语义地址。码本只在训练允许的商品内容上学习。建议首轮尝试 3 层、每层 256 个码字，最终选择由验证集决定，不能预先把这些数值当成最优。

码本冻结后，新商品可以被映射到已有 token 空间。保留语义 ID 到商品集合的映射，显式记录碰撞。TIGER 原始复现遵循其唯一化方法；项目动态实现若改用“语义桶生成 + 桶内排序”，必须标为适配版本。

动态实现中，同一语义 ID 对应多个商品时，使用共同的内容匹配分数在桶内截断；不能在离线评估时把真实目标直接补入候选。桶扫描与桶内排序均计入预算，超大桶必须有确定的上限与溢出日志。

### 8.2 生成式检索

用户过去交互的语义 ID 序列进入编码器，解码器预测下一商品的语义 token。训练采用逐 token 的交叉熵；推理使用当前物品库的前缀树约束，返回合法语义路径，再映射为候选商品。

生成式候选分数使用路径对数概率，不直接与稠密向量点积相加。不同语义桶的大小可能影响结果，需通过分桶报告与消融检查。主实验冻结长度归一化、去重及同分排序规则。

### 8.3 请求级预算路由

路由器在昂贵检索前选择动作。初始动作集如下，数值是试验起点；最终每个动作的真实成本由性能测量确定。

| 动作 | 检索路径 | 最大合并候选数 |
| --- | --- | --- |
| D | 稠密检索 200 个候选 | 200 |
| G | 生成式检索并映射商品 | 200 |
| H | 生成式 100 个与稠密 100 个合并 | 200 |

所有动作在相同候选集合接口后使用同一个内容感知评分器输出 Top 10，因此主消融聚焦候选检索与预算。纯生成式直接排序作为额外结果单独呈现。本项目不把这条混合链路称为完全单阶段推荐。

初始路由特征包括历史长度、历史训练频次分布、最近行为间隔、历史中未训练商品占比、当前新增商品比例以及可复用的用户向量。所有统计有明确截止时刻。

禁止使用当前真实目标、是否命中、目标冷暖标签或尚未计算的昂贵检索结果。若使用生成器熵或前缀支持作为探测特征，必须先执行探测，并把探测时间和 GPU 开销计入总成本；不可把两路都跑完再称为节省检索。

### 8.4 路由训练

在训练区间内做滚动交叉拟合：较早历史训练基础模型；对随后一段请求离线运行所有候选动作，记录各动作的推荐效果与实际成本；继续向后滚动。预测一个训练样本的基础模型不得见过它的目标。D 轨的码本在最早拟合窗口训练后冻结，后续窗口仅编码新商品；训练统计和索引均按各折的时间边界重建。

用这些样本训练一个小型多层感知机，预测动作效用或动作选择。概念目标为“推荐质量减去计算成本惩罚”，惩罚权重在验证集选择。不要把研究问题复杂化为必须使用强化学习；监督路由是首个待验证方案。

最后，基础检索模型用完整训练区间重新拟合，路由器保留滚动样本训练结果，并在验证期校准。基础模型变化会使路由特征分布变化，必须检查校准误差；若明显失效，保留固定混合方案并说明原因。

基础模型、路由器和固定策略均不读取验证或测试真值拟合参数。验证真值只用于事先限定的模型选择；各基线同样有完整训练区间和调参机会。

### 8.5 合法性与回退

所有结果必须属于请求捕获的有效物品库、去重且符合已交互过滤规则。候选不足时优先以当前库中的稠密或热门结果补齐，并记录回退；禁止静默伪装为纯生成式成功。

无历史用户返回当前可用热门与类别多样化列表；内容缺失新商品单独分组；无法取得有效模型时返回服务不可用状态。超时允许使用已完成的候选，响应必须说明 degraded 与实际动作。

## 09 功能需求与验收

优先级 P0 表示形成研究结论所必需，P1 表示增强作品展示与工程深度，P2 表示独立扩展。以下需求均为待实现。

| 编号 | 级别 | 功能 | 验收条件 |
| --- | --- | --- | --- |
| F01 | P0 | 数据导入与数据卡 | 相同输入与配置产生相同哈希及过滤统计 |
| F02 | P0 | 时间重放 | 修改未来行为不改变此前请求的输入与物品库 |
| F03 | P0 | 基线训练 | 每个模型保存配置、提交号、种子与权重 |
| F04 | P0 | 语义编码与诊断 | 能导出 token 使用率、碰撞率及路径支持统计 |
| F05 | P0 | 公共候选接口 | 每条候选携带来源，禁止补入评估真值 |
| F06 | P0 | 路由与成本记录 | 路由只依赖允许特征，探测成本完整计入 |
| F07 | P0 | 统一离线评估 | 所有方法使用同一事件集合与物品库快照 |
| F08 | P0 | 增量商品入库 | 冻结模型与码本，新商品可加入合法候选库 |
| F09 | P0 | 快照发布与回滚 | 请求期间索引与映射版本一致，回滚可复现 |
| F10 | P1 | 商品推荐演示 | 展示历史、候选来源与最终 Top 10 |
| F11 | P1 | 实验工作台 | 展示指标、分组、资源成本和失败案例 |
| F12 | P1 | C++ 关键路径 | 与参考实现结果一致，并给出实测收益 |
| F13 | P2 | 外部大模型迁移 | 单列训练来源、可能的数据重叠与资源成本 |

### 页面行为

推荐页包含用户或示例历史选择、Top 10 商品与可选策略对比。算法名称与分数放在“研究详情”中，普通浏览区展示标题、类别和历史关联信息。历史关联只是解释性展示，不把注意力权重当作因果解释。

物品库演示页支持从自制示例商品文件导入一批新商品，显示构建状态、当前版本和是否已生效。更新失败保留旧版本，并展示可理解的错误原因。

实验页从真实报告文件读取结果；未运行的表格显示“未运行”，不能预填预期收益。允许按 Warm、Model-cold、Tail 和时间窗口筛选，并始终显示样本数。

## 10 系统结构与接口

离线流程为：原始交互与元数据 → 时间切分与数据卡 → 内容编码与语义码本 → 基线及路由训练 → 冻结模型包 → 统一评估与报告。

在线演示流程为：请求与过去历史 → 捕获物品库快照 → 路由动作 → 生成式或稠密检索 → 统一评分与过滤 → Top 10 与运行记录。

先使用模块清晰的 Python 单服务，模型、评估器和数据处理保持独立。研究稳定后再加入 C++ 组件。前端保持简洁，模型实验不依赖前端才能运行。

| 接口 | 用途 | 关键输入或输出 |
| --- | --- | --- |
| POST /v1/recommend | 生成推荐 | user_id、history、as_of、top_k、budget_tier |
| POST /v1/catalog/batches | 提交演示商品批次 | batch_id、items、content_schema |
| GET /v1/catalog/jobs/{id} | 查询构建进度 | pending、building、validated、active、failed |
| POST /v1/catalog/activate | 激活已验证快照 | snapshot_id、expected_previous_id |
| POST /v1/catalog/rollback | 回退到上一有效快照 | snapshot_id |
| GET /v1/experiments/{id} | 读取实验报告 | metrics、slices、manifest、artifacts |
| GET /health | 检查就绪状态 | model_version、snapshot_id、ready |

recommend 响应包含 request_id、model_version、tokenizer_version、snapshot_id、selected_action、actual_action、latency_ms、degraded 和 items。每个 item 包含稳定主键、rank、score 和 retrieval_sources。score 是模型匹配分数，不标为点击概率。

top_k 默认 10，范围 1–50；history 超长按已公布规则截断。未知版本、非法时间和格式错误返回明确错误。管理员接口仅用于本地或受保护的演示环境。

模型包包含模型权重、内容编码器版本、码本、特征 schema、tokenizer、配置与训练截止时间。物品库快照包含 SID 映射、前缀树、稠密索引、元数据与有效性过滤状态，并声明兼容的模型和码本版本。

新商品更新先在旁路构建完整快照，完成一致性校验后原子切换。正在处理的请求继续持有旧快照，直到请求结束才释放；同一次请求不能混用新旧前缀树和物品映射。

## 11 指标与统计口径

### 推荐质量

主指标为全事件 NDCG@10；同时报告目标已入库事件 NDCG@10、Recall@20 和候选 Recall@200。单个请求只有一个下一交互目标时，Recall@K 与 HitRate@K 等价，不把它们当作两个独立收益。

目标排名为 r 且 r ≤ K 时，单目标 NDCG@K 为 1 / log2(r + 1)，否则为 0。全事件口径中尚未入库目标记为 0；条件口径剔除此类事件，但必须明确分母与占比。

先计算每位用户在评估期内各请求的平均值，再做用户平均，作为主汇总；事件平均作为补充。Warm、Model-cold 与 Tail 使用相同汇总规则，显示用户数、事件数和可推荐目标覆盖率。

### 诊断与成本

| 指标 | 用途 |
| --- | --- |
| 结构可达目标比例 | 检查有效库、可表示 ID、前缀树和映射是否允许目标 |
| 前缀支持分布 | 检查低频路径与召回失败的关系 |
| 无效输出率与回退率 | 揭示非法路径、候选不足和故障 |
| Item Coverage@10 | 至少推荐一次的商品数占同期可用商品并集的比例 |
| 路由动作占比 | 检查是否退化为固定动作 |
| p50 与 p95 延迟 | 衡量单请求与尾部响应时间 |
| 吞吐与 GPU 时间 | 衡量并发能力及计算资源消耗 |
| 新物品入库耗时 | 从提交到新快照可用，内容编码单独拆分 |

不使用全库均匀负采样 AUC 作为主结果。精确全库评估与 ANN 评估必须分开；若使用采样评估做调试，标注候选采样规则，不与主表混用。

### 统计要求

主实验至少三个随机种子，保留每个种子的原始值、均值与标准差。以用户为簇做配对 bootstrap，报告改进差值的 95% 置信区间；随机种子间波动另外报告，不把两类不确定性混成一个数字。

预先固定主要比较与主要指标。其他分组和大量消融属于探索性结果，不能在几十个比较中只选显著的几个宣称普遍有效。冷组样本不足时只做描述，不强行报告稳定优势。

## 12 实验矩阵与成功条件

### 核心实验

| 编号 | 实验 | 要回答的问题 |
| --- | --- | --- |
| E01 | 官方复现 | 代码能否达到相近结果，偏差来自哪里 |
| E02 | 全局时间与动态库比较 | 切换到真实约束代理后模型排序是否变化 |
| E03 | 六类基线与最强固定混合 | 改进是否超越有竞争力的对手 |
| E04 | 学习路由与规则路由 | 模型选择是否比简单规则更有价值 |
| E05 | 去掉时间与物品库特征 | 收益是否来自动态环境信息 |
| E06 | 无探测与有成本探测 | 额外探测是否值得付出开销 |
| E07 | 改变 beam 与 ANN 深度 | 收益是否仅来自更多检索计算 |
| E08 | 冷暖与路径支持分组 | 对谁有效，对谁无效，机制是否一致 |
| E09 | 多快照连续重放 | 冻结模型面对新增商品是否逐渐退化 |
| E10 | 第二领域与三个种子 | 收益是否可以稳定复现 |

每项实验保存唯一 run_id、代码提交号、数据与物品库哈希、切分配置、种子、超参数、硬件、运行时长和结果路径。失败运行也保存记录。

### 目标不是既成事实

研究目标 A：在与最强固定策略相同的 p95 延迟约束下，提高 Model-cold Recall@20，并保持整体 NDCG@10。

研究目标 B：在整体 NDCG@10 相对退化不超过 1% 的预先声明容忍范围内，相比最强固定混合降低至少 20% 的平均计算时间。

上述阈值是启动时的目标，不是保证达成的验收数字。研究结论以置信区间和实际成本为准；“均值看起来更高”不等于稳定改进。即使 A 与 B 均未达成，也要完成结果分析，不能反复试探测试集后改口径。

### 完成与优秀的区别

完成线是协议正确、基线可信、主实验可复现、方法局限交代清楚。具有明显竞争力的作品还应做到：至少一个核心假设获得跨种子支持，与最接近强基线的差异能解释，第二领域有验证，且第三方可以复现关键结果。

投稿级目标还需要系统文献审查、对已有方法的充分比较和更强实验支撑；本 PRD 不预判项目能达到投稿标准。

## 13 性能与工程验收

质量比较先使用同一硬件和相同请求集合，分别测量 batch=1 延迟以及固定批量吞吐。预热、数据传输、CPU 预处理、路由、探测、检索和排序都要列出计时边界。GPU 异步操作必须同步或使用正确事件计时。

演示初始目标为 Top 10 请求 p95 小于 300 ms，错误率低于 1%；这些数值只有在指定硬件、模型规模、候选上限和负载下才有意义，首轮测量后冻结验收配置。训练任务与性能测试隔离，禁止将单请求峰值包装成持续吞吐。

C++ 版本先保证与 Python 参考实现输出一致，再比较速度。测试涵盖空前缀、碰撞桶、无效商品、同分稳定顺序、快照切换和并发读。若没有实测优势，保留参考实现并解释瓶颈。

必须自动验证的边界包括：未来行为不能改变过去结果；当前目标不进入路由输入；真实目标不被候选阶段强行补齐；内容编码与码本版本不兼容时拒绝激活；回滚后相同请求产生相同的确定性结果。

## 14 里程碑与资源计划

不以最低难度作为选题约束，但仍按依赖顺序推进。参考工作量为 14–20 周，每周约 25–35 小时；这是规划估计，不是对你现有基础或设备的判断。若 PyTorch、概率和优化基础尚未建立，需先补相应阶段。

| 阶段 | 参考时间 | 必须完成的结果 | 进入下一阶段的条件 |
| --- | --- | --- | --- |
| M0 | 第 1–2 周 | 理解任务、阅读核心论文、数据卡草案 | 能手算交叉熵与 NDCG，解释数据泄漏 |
| M1 | 第 3–4 周 | 数据协议、热门与 ItemCF、测试 | 时间与候选规则通过自动检查 |
| M2 | 第 5–7 周 | SASRec-CE、稠密检索、TIGER 与 LIGER | 基线偏差可解释，结果可重跑 |
| M3 | 第 8–10 周 | 动态库重放与可达性分析 | 得到值得研究的差异或明确否定证据 |
| M4 | 第 11–13 周 | 滚动路由训练与核心消融 | 与固定混合在同等预算下比较 |
| M5 | 第 14–16 周 | 第二领域、统计检验、失败分析 | 结论不依赖单种子或单数据切片 |
| M6 | 第 17–20 周 | C++ 优化、演示、论文式报告与复现 | 他人可运行关键实验并理解贡献 |

### 计算资源

初始实验以中小规模推荐 Transformer 为主，模型参数规模由学习曲线与基线配置决定，不强制从十亿参数模型起步。单张 24 GB 显存 GPU 可作为首轮估算配置，实际 batch 与序列长度须通过 smoke run 确认；完整多种子、多领域实验建议安排专门算力。

若加入 1.7B 或更大预训练模型迁移，必须独立规划显存、微调方式、时间与成本，不能把现成预训练带来的收益全部归因于本项目方法。OpenOneRec 发布的不同模型有不同数据来源，使用时须检查权重与目标数据的关系。

先测一个 epoch、一次全库评估、一次文本编码与一次路由数据采集。总预算按“试验数 × 每试验实测小时 + 编码 + 路由数据 + 最终多种子验证”估算，并保留约 20% 余量。GPU 小时与价格在选定设备后填写，不虚构总价。

### 学习优先级

优先掌握线性代数中的向量与矩阵运算、概率中的条件概率和期望、梯度与优化、交叉熵、负采样偏差、PyTorch 数据管道、Transformer 掩码与训练循环。随后学习 RQ-VAE、束搜索、ANN 和实验统计。

你的 C++ 基础会帮助理解数据结构与性能，但推荐算法岗位仍需要你能独立阅读和修改 Python 模型、排查训练问题。项目中每个核心模块都应有自己的推导与调试记录。

## 15 风险与停止规则

| 风险 | 识别信号 | 应对措施 |
| --- | --- | --- |
| 研究点与已有工作重复 | 相关论文已有同一机制和协议 | 更新贡献定位，加入最接近对照 |
| 基线过弱产生虚假收益 | 简单调参即可消除优势 | 先修复基线，再继续新模型 |
| 冷启动样本定义错误 | 首次目标被提前加入候选库 | 修正协议，作废受影响结果 |
| 路由使用未来信息 | 特征依赖当前答案或全量统计 | 增加时间隔离测试并重做实验 |
| 路由只在训练窗口有效 | 验证阶段退化或动作塌缩 | 校准或采用固定混合，保留负结果 |
| 算力被大模型消耗 | 尚无可靠基线就开始大规模训练 | 回到能检验假设的最小有效规模 |
| 离线收益被误作业务收益 | 报告出现未经验证的 CTR 提升 | 更正为对应离线指标和人群 |
| 前端或部署挤占研究 | 页面完善但没有消融报告 | 先完成 M5，再做展示增强 |

若两轮预先设定的路由方案在验证集均不优于最强固定混合，停止无上限增加网络复杂度；分析数据分组、计算成本和可达性瓶颈，形成明确结论。测试集不用于反复寻找下一轮改进方向。

## 16 展示与面试验收

### 三分钟演示脚本

第一分钟展示一个带时间的真实历史样例、可用商品库和推荐结果；解释新商品为什么没有训练交互，以及什么情况下它仍能被内容模型找到。

第二分钟重放一次物品库更新，展示生成式、稠密和路由策略的候选差异、合法性检查与耗时。样例提前冻结，同时准备一个失败样例，避免只选成功案例。

第三分钟展示主结果表、最重要的消融与置信区间；说明哪些结果来自个人改进、哪些来自已有代码，以及当前还不能证明什么。

### 必须能回答的问题

1. 生成式推荐与用大模型写推荐理由有什么区别？
2. 语义 ID 为何会碰撞，约束解码为何不能保证推荐到所有新商品？
3. 你的冷启动定义与真正零反馈上架有什么区别？
4. 为什么不用随机切分，为什么不能把目标商品补进候选？
5. 稠密基线和 LIGER 已经很强，你的新增贡献在哪里？
6. 路由器如何学习动作选择，它看到的信息在预测时是否真的可用？
7. 怎样证明收益不是更多候选、更大模型或额外计算造成的？
8. 如果整体指标下降、冷组提升，你如何决定是否部署？
9. 三个种子与用户 bootstrap 分别说明什么？
10. C++ 优化减少了哪一部分耗时，端到端收益是多少？

### 简历表述规则

完成后可按以下结构写，方括号内容全部来自报告，未实测项不填：

“围绕动态物品库下的生成式推荐，构建全局时间重放与冷物品评估协议，复现 SASRec、TIGER、LIGER 等基线；设计基于请求历史与物品库状态的预算路由，在［数据领域与协议］、［硬件与预算］下，取得［离线指标差值和统计结果］，并完成［具体 C++ 组件］的推理优化。”

若改进未获得稳定支持，可写复现、诊断和系统结论，不能填写预计收益。不得出现未经验证的“工业级百万 QPS”“线上 CTR 提升”或“首次提出”。

## 17 开发启动任务

第一批工作按依赖顺序执行，完成后再进入模型创新。

| 顺序 | 任务 | 产物 |
| --- | --- | --- |
| 1 | 建立研究仓库与许可证清单 | README、环境文件、第三方来源说明 |
| 2 | 下载单一领域并统计字段 | data_card.md、数据 manifest |
| 3 | 实现两个评估轨与候选库重放 | protocol.yaml、时间与泄漏测试 |
| 4 | 跑通 Popularity 与 ItemCF | 第一张真实基线结果表 |
| 5 | 复现稠密模型与 SASRec-CE | 可重复训练和全库评估 |
| 6 | 复现 TIGER 与 LIGER | 官方协议复现差异报告 |
| 7 | 诊断新商品与语义路径 | 冷组报告、可达性与前缀图表 |
| 8 | 冻结研究假设与主比较 | experiment_plan.yaml |

建议目录为 data_pipeline、models、routing、evaluation、catalog、serving、configs、tests 和 reports。目录体现问题边界，不提前拆成大量微服务。

## 18 参考资料与使用方式

以下来源于 2026 年 9 月 13–14 日核查。论文结论以其数据、实现和实验条件为限；近期预印本不等同已经同行评审。这里列出的资料是选题与设计依据，不是本项目已完成的实验。

1. [百度推荐算法实习岗位](https://talent.baidu.com/jobs/detail/INTERN/f7769b9b-2597-4d5f-a7a7-88705d38cf2a)：理解岗位涉及的行为建模和算法基础；投递时重新核实状态。
2. [TIGER 论文](https://arxiv.org/abs/2305.05065)：理解语义 ID 与生成式检索的基本路线。
3. [LIGER 官方代码](https://github.com/facebookresearch/liger)：作为最接近的混合检索基线；其代码和子模块许可须分别检查。
4. [语义 ID 冷启动的时间视角](https://arxiv.org/abs/2607.21101)：近期预印本，辅助设计可达性和冷组诊断。
5. [OpenOneRec 官方项目](https://github.com/Kuaishou-OneRec/OpenOneRec)：了解工业研究方向与开放模型，单独检查训练来源与权重许可。
6. [Meta Generative Recommenders](https://github.com/meta-recsys/generative-recommenders)：了解 HSTU 等架构与系统实践。
7. [Amazon Reviews 2023 数据说明](https://amazon-reviews-2023.github.io/main.html)：核查交互与商品字段、数据获取方式。
8. [Amazon 官方预处理与切分](https://github.com/hyp1231/AmazonReviews2023/blob/main/benchmark_scripts/README.md)：理解去重、过滤和全局时间切分。
9. [SASRec 原始论文](https://arxiv.org/abs/1808.09781)：建立序列推荐基线的原理认识。
10. [Time to Split](https://doi.org/10.1145/3705328.3748164)：检查序列推荐的离线评估协议与强基线问题。
11. [Faiss 官方文档](https://github.com/facebookresearch/faiss/wiki)：实现与验证稠密向量检索。

文档验收：目标、范围、数据合同、算法假设、最接近基线、功能、接口、指标、实验、资源、风险和最终交付均已定义。项目代码、模型训练和实测报告属于后续实施阶段。
